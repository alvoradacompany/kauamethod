-- Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Cria a ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "UpdateMessage"
screenGui.ResetOnSpawn = false
screenGui.Parent = PlayerGui

-- Cria o texto da mensagem
local messageLabel = Instance.new("TextLabel")
messageLabel.Size = UDim2.new(0, 400, 0, 100)
messageLabel.Position = UDim2.new(0.5, -200, 0.5, -50) -- centralizado
messageLabel.BackgroundTransparency = 1
messageLabel.Text = "Tá off pra atualizações"
messageLabel.TextColor3 = Color3.fromRGB(0, 255, 0) -- verde
messageLabel.TextScaled = true
messageLabel.Font = Enum.Font.SourceSansBold
messageLabel.Parent = screenGui
